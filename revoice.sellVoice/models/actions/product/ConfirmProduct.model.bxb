action (ConfirmProduct) {
  description ("사용자 확인")
  type(Search)
  collect {
    input (searchProductResult) {
      type (SearchProductResultConcept)
      min (Required) max (One)
    }
  }
  output (ConfirmProductResultConcept)
}
